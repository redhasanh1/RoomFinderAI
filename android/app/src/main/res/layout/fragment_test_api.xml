<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="API Test Panel"
            android:textSize="24sp"
            android:textStyle="bold"
            android:layout_marginBottom="16dp"
            android:textColor="@color/purple_primary" />

        <!-- Test Listings API -->
        <Button
            android:id="@+id/testListingsButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Test GET Listings API"
            android:layout_marginBottom="8dp" />

        <!-- Test Create Listing -->
        <Button
            android:id="@+id/testCreateListingButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Test POST Listing API"
            android:layout_marginBottom="8dp" />

        <!-- Test AI Chat -->
        <Button
            android:id="@+id/testAIChatButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Test AI Chat API (OpenAI)"
            android:layout_marginBottom="8dp" />

        <!-- Test Configuration -->
        <Button
            android:id="@+id/testConfigButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Test Config API"
            android:layout_marginBottom="8dp" />

        <!-- Test Search -->
        <Button
            android:id="@+id/testSearchButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Test Search Listings"
            android:layout_marginBottom="16dp" />

        <!-- Progress Bar -->
        <ProgressBar
            android:id="@+id/progressBar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:visibility="gone"
            android:layout_marginBottom="16dp" />

        <!-- Results Display -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Results:"
            android:textSize="18sp"
            android:textStyle="bold"
            android:layout_marginBottom="8dp" />

        <TextView
            android:id="@+id/resultTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Click a button to test an API endpoint"
            android:textSize="14sp"
            android:padding="12dp"
            android:background="#f0f0f0"
            android:textColor="#333"
            android:fontFamily="monospace"
            android:scrollbars="vertical"
            android:maxLines="20" />

    </LinearLayout>
</ScrollView>