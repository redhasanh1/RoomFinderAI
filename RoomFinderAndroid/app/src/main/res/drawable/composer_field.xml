<?xml version="1.0" encoding="utf-8"?>
<!-- Composer field: a quiet filled capsule, so the send button is the only
     thing on the bar competing for attention. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/ios_gray6" />
    <stroke android:width="1dp" android:color="@color/ios_gray4" />
    <corners android:radius="25dp" />
</shape>
