<?xml version="1.0" encoding="utf-8"?>
<!-- A search field: white, hairline edge. The 2dp brand-tinted border it had
     made an input look like the primary action on the screen. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/surface_card" />
    <stroke android:width="1dp" android:color="@color/separator_strong" />
    <corners android:radius="26dp" />
</shape>
