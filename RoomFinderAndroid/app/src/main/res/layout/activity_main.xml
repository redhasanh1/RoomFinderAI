<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <!--
        No bottom margin here on purpose. MainActivity.applySystemBarInsets()
        sets this container's bottom padding to the tab bar's own measured
        height, which already covers the gesture bar on every device.

        A 72dp margin used to sit here as well, so the space below the content
        was reserved twice: 72dp of margin plus 110dp of padding against a tab
        bar that is only 110dp tall. The leftover 72dp showed as a dead band
        above the menu on every single tab.
    -->
    <FrameLayout
        android:id="@+id/fragmentContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <View
        android:id="@+id/tabBarSeparator"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_gravity="bottom"
        android:background="@color/separator" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="@color/surface_card"
        android:paddingTop="6dp"
        app:elevation="0dp"
        app:itemActiveIndicatorStyle="@style/TabBar.ActiveIndicator"
        app:itemIconSize="24dp"
        app:itemIconTint="@color/bottom_nav_selector"
        app:itemTextAppearanceActive="@style/Text.TabLabel.Active"
        app:itemTextAppearanceInactive="@style/Text.TabLabel"
        app:itemTextColor="@color/bottom_nav_selector"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/bottom_navigation_menu" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
