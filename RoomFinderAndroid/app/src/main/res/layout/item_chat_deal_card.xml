<?xml version="1.0" encoding="utf-8"?>
<!--
  Shown when a landlord agrees.

  Mirrors the iOS deal card. Android used to announce a win as a line of chat -
  "Negotiation Success! I've successfully negotiated terms for listing 4f2a..." -
  which gives a database id and no money. The number is the entire point of the
  feature and it was the one thing missing.

  Everything below the headline is optional and hidden when absent: a landlord
  can agree without a figure, or agree a viewing and nothing else, and the card
  is still worth showing rather than padded with blanks.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="16dp"
    android:paddingVertical="6dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/deal_card_gradient"
        android:orientation="vertical"
        android:padding="16dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <ImageView
                android:layout_width="22dp"
                android:layout_height="22dp"
                android:contentDescription="@null"
                android:src="@drawable/ic_check_circle"
                android:tint="@color/white" />

            <TextView
                android:id="@+id/dealHeadline"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="10dp"
                android:layout_weight="1"
                android:fontFamily="sans-serif-medium"
                android:text="We secured it"
                android:textColor="@color/white"
                android:textSize="17sp" />
        </LinearLayout>

        <TextView
            android:id="@+id/dealRoom"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:fontFamily="sans-serif-medium"
            android:textColor="@color/white"
            android:textSize="14sp" />

        <!-- The saving, stated per month and per year. A year figure is what
             makes a $75 discount feel like the $900 it actually is. -->
        <TextView
            android:id="@+id/dealSaving"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:alpha="0.92"
            android:textColor="@color/white"
            android:textSize="13sp"
            android:visibility="gone" />

        <TextView
            android:id="@+id/dealViewing"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:alpha="0.92"
            android:textColor="@color/white"
            android:textSize="13sp"
            android:visibility="gone" />

    </LinearLayout>
</LinearLayout>
