# Nixpacks configuration for RoomFinderAI
[start]
cmd = "node frontend/backend/server.js"

[build]
cmds = [
    "npm install --production",
    "npm install --prefix frontend/backend --production"
]

[variables]
NODE_ENV = "production"
NPM_CONFIG_PRODUCTION = "true"