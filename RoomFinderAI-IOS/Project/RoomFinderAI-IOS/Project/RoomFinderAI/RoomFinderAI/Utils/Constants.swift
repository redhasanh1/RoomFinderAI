import Foundation

struct Constants {
    // Simplified constants for demo app
    
    struct UserDefaults {
        static let hasSeenOnboarding = "has_seen_onboarding"
        static let darkModeEnabled = "dark_mode_enabled"
    }
}